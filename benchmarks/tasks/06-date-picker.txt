Add a date picker to this HTML form. Users need to select a date of birth on a registration page. The date should be in the past (no future dates) and the user must be at least 13 years old.

Write ONLY the date input HTML and any minimal JS needed. Assume a modern browser.
